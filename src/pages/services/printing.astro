---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import RecentProjects from '../../components/RecentProjects.astro';
import { Printer, Check, ArrowRight, FileText, Settings, Clock, Leaf, Headphones, Calculator } from 'lucide-astro';

const features = [
  {
    icon: Calculator,
    title: "No Upfront Investment",
    description: "Get premium printing equipment without large capital expenditure."
  },
  {
    icon: Settings,
    title: "Maintenance Included",
    description: "All repairs, parts, and servicing covered in your lease agreement."
  },
  {
    icon: Clock,
    title: "Latest Technology",
    description: "Upgrade to newer models as technology advances during your lease."
  },
  {
    icon: FileText,
    title: "Flexible Terms",
    description: "Customizable lease periods from 12 to 60 months to fit your needs."
  },
  {
    icon: Leaf,
    title: "Eco-Friendly Options",
    description: "Energy-efficient models and recycling programs available."
  },
  {
    icon: Headphones,
    title: "Dedicated Support",
    description: "Priority technical support and quick response times."
  }
];

const specifications = [
  "Multifunction printers (MFP)",
  "Color laser printers",
  "High-volume production printers",
  "Wide format printers",
  "Document scanners",
  "Copiers",
  "Toner & supplies included",
  "Fleet management software"
];

const applications = [
  { title: "Corporate Offices", description: "Departmental and workgroup printing solutions" },
  { title: "Print Shops", description: "High-volume production equipment for commercial printing" },
  { title: "Educational Institutions", description: "Campus-wide printing and copying services" },
  { title: "Healthcare Facilities", description: "Secure document handling and patient records" }
];

const plans = [
  {
    name: "Starter",
    description: "Perfect for small offices",
    features: ["Desktop printer", "Up to 5,000 pages/month", "Basic maintenance", "Email support"]
  },
  {
    name: "Business",
    description: "Ideal for growing companies",
    features: ["Multifunction printer", "Up to 20,000 pages/month", "Full maintenance", "Priority support", "Toner included"]
  },
  {
    name: "Enterprise",
    description: "For high-volume needs",
    features: ["Production printer", "Unlimited pages", "Same-day service", "Dedicated account manager", "Fleet management"]
  }
];
---

<Layout
  title="Printing Equipment Leasing - OptimuxSolutions"
  description="Professional printing equipment leasing services. Multifunction printers, copiers, and production equipment with maintenance included."
>
  <Header />

  <main class="flex-1">
    <!-- Hero Section -->
    <section class="relative py-20 md:py-28 overflow-hidden">
      <div class="absolute inset-0 bg-[#1c6cb1]">
        <div class="absolute inset-0">
          <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice">
            <defs>
              <pattern id="hero-grid" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
                <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#ffffff" stroke-width="0.5" opacity="0.1"/>
              </pattern>
              <pattern id="hero-dots" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse">
                <circle cx="2" cy="2" r="1" fill="#ffffff" opacity="0.15"/>
              </pattern>
            </defs>
            <rect width="100%" height="100%" fill="url(#hero-grid)"/>
            <rect width="100%" height="100%" fill="url(#hero-dots)"/>
          </svg>
        </div>
      </div>

      <div class="absolute top-0 right-0 w-96 h-96 bg-[#5a9fd4]/30 rounded-full blur-[120px]"></div>
      <div class="absolute bottom-0 left-0 w-80 h-80 bg-[#155a94]/50 rounded-full blur-[100px]"></div>

      <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <div>
            <a href="/services" class="inline-flex items-center gap-2 text-white/80 hover:text-white mb-6 transition-colors">
              <ArrowRight class="w-4 h-4 rotate-180" />
              <span class="text-sm font-medium">Back to Services</span>
            </a>

            <div class="flex items-center gap-4 mb-6">
              <div class="w-16 h-16 bg-white/10 backdrop-blur-sm rounded-2xl flex items-center justify-center">
                <Printer class="w-8 h-8 text-white" />
              </div>
              <div class="h-px bg-white/20 flex-1"></div>
            </div>

            <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight" style="color: white;">
              Printing Equipment<br/>Leasing
            </h1>
            <p class="text-lg md:text-xl leading-relaxed mb-8" style="color: rgba(255,255,255,0.85);">
              Professional printing equipment leasing for businesses of all sizes. Get access to the latest technology without the upfront investment, with maintenance included.
            </p>

            <div class="flex flex-col sm:flex-row gap-4">
              <a
                href="/contact"
                class="inline-flex items-center justify-center gap-2 bg-white hover:bg-white/90 text-[#1c6cb1] px-8 py-4 rounded-xl font-bold text-lg transition-all shadow-lg"
              >
                Get a Quote
                <ArrowRight class="w-5 h-5" />
              </a>
              <a
                href="#features"
                class="inline-flex items-center justify-center gap-2 bg-white/15 hover:bg-white/25 backdrop-blur-sm border border-white/30 px-8 py-4 rounded-xl font-bold text-lg transition-all"
                style="color: white;"
              >
                Learn More
              </a>
            </div>
          </div>

          <div class="relative">
            <div class="aspect-[4/3] rounded-3xl overflow-hidden bg-white/10 backdrop-blur-sm border border-white/20">
              <svg class="w-full h-full" viewBox="0 0 400 300" xmlns="http://www.w3.org/2000/svg">
                <defs>
                  <pattern id="print-grid" width="20" height="20" patternUnits="userSpaceOnUse">
                    <path d="M 20 0 L 0 0 0 20" fill="none" stroke="#ffffff" stroke-width="0.5" opacity="0.1"/>
                  </pattern>
                </defs>
                <rect width="100%" height="100%" fill="url(#print-grid)"/>

                <!-- Printer Machine -->
                <g transform="translate(110, 50)">
                  <!-- Main body -->
                  <rect x="20" y="50" width="140" height="100" rx="8" fill="#ffffff" opacity="0.2" stroke="#ffffff" stroke-width="2" opacity="0.3"/>
                  <!-- Top cover -->
                  <rect x="30" y="40" width="120" height="20" rx="4" fill="#ffffff" opacity="0.15"/>
                  <!-- Control panel -->
                  <rect x="120" y="70" width="30" height="40" rx="3" fill="#ffffff" opacity="0.2"/>
                  <!-- Display -->
                  <rect x="125" y="75" width="20" height="12" rx="2" fill="#ffffff" opacity="0.25"/>
                  <!-- Paper tray -->
                  <rect x="40" y="130" width="80" height="15" rx="2" fill="#ffffff" opacity="0.15"/>
                  <!-- Output tray -->
                  <rect x="40" y="55" width="70" height="8" rx="1" fill="#ffffff" opacity="0.1"/>
                  <!-- Paper stack -->
                  <rect x="45" y="48" width="50" height="6" fill="#ffffff" opacity="0.2"/>
                  <rect x="47" y="45" width="46" height="6" fill="#ffffff" opacity="0.15"/>
                </g>

                <circle cx="80" cy="80" r="30" fill="#5a9fd4" opacity="0.2"/>
                <circle cx="320" cy="220" r="50" fill="#ffffff" opacity="0.1"/>

                <!-- Status -->
                <circle cx="350" cy="40" r="6" fill="#22c55e" opacity="0.8"/>
                <text x="310" y="45" fill="#ffffff" opacity="0.6" font-size="11" font-family="sans-serif">READY</text>
              </svg>

              <div class="absolute bottom-6 left-6 right-6 flex gap-4">
                <div class="bg-white/95 backdrop-blur-sm rounded-xl px-4 py-3 shadow-lg flex-1">
                  <p class="text-2xl font-bold text-[#1c6cb1]">$0</p>
                  <p class="text-xs text-slate-600">Upfront</p>
                </div>
                <div class="bg-white/95 backdrop-blur-sm rounded-xl px-4 py-3 shadow-lg flex-1">
                  <p class="text-2xl font-bold text-[#1c6cb1]">100%</p>
                  <p class="text-xs text-slate-600">Maintained</p>
                </div>
                <div class="bg-white/95 backdrop-blur-sm rounded-xl px-4 py-3 shadow-lg flex-1">
                  <p class="text-2xl font-bold text-[#1c6cb1]">Flex</p>
                  <p class="text-xs text-slate-600">Terms</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Grid -->
    <section id="features" class="section-padding bg-slate-50 bg-pattern-curves">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 mb-4">
            <span class="w-2 h-2 rounded-full bg-primary"></span>
            <span class="text-sm font-semibold text-primary">Benefits</span>
          </div>
          <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-slate-900 mb-4">
            Why Lease With Us
          </h2>
          <p class="text-lg text-slate-600 max-w-2xl mx-auto">
            Our leasing programs are designed to give you the best equipment with maximum flexibility.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {features.map((feature) => (
            <div class="bg-white rounded-2xl p-8 shadow-lg shadow-slate-200/50 hover:shadow-xl hover:shadow-slate-200/50 transition-all duration-300 group">
              <div class="w-14 h-14 bg-primary/10 rounded-2xl flex items-center justify-center mb-6 group-hover:bg-primary group-hover:scale-110 transition-all">
                <feature.icon class="w-7 h-7 text-primary group-hover:text-white transition-colors" />
              </div>
              <h3 class="text-xl font-bold text-slate-900 mb-3">{feature.title}</h3>
              <p class="text-slate-600 leading-relaxed">{feature.description}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Leasing Plans -->
    <section class="section-padding bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 mb-4">
            <span class="w-2 h-2 rounded-full bg-primary"></span>
            <span class="text-sm font-semibold text-primary">Leasing Plans</span>
          </div>
          <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-slate-900 mb-4">
            Choose Your Plan
          </h2>
          <p class="text-lg text-slate-600 max-w-2xl mx-auto">
            Flexible leasing options tailored to your printing needs and budget.
          </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
          {plans.map((plan, index) => (
            <div class={`rounded-2xl p-8 ${index === 1 ? 'bg-primary text-white shadow-xl shadow-primary/30 scale-105' : 'bg-slate-50'}`}>
              <h3 class={`text-2xl font-bold mb-2 ${index === 1 ? 'text-white' : 'text-slate-900'}`}>{plan.name}</h3>
              <p class={`mb-6 ${index === 1 ? 'text-white/80' : 'text-slate-600'}`}>{plan.description}</p>

              <ul class="space-y-3 mb-8">
                {plan.features.map((feature) => (
                  <li class="flex items-center gap-3">
                    <div class={`w-5 h-5 rounded-full flex items-center justify-center ${index === 1 ? 'bg-white/20' : 'bg-primary/10'}`}>
                      <Check class={`w-3 h-3 ${index === 1 ? 'text-white' : 'text-primary'}`} />
                    </div>
                    <span class={index === 1 ? 'text-white/90' : 'text-slate-700'}>{feature}</span>
                  </li>
                ))}
              </ul>

              <a
                href="/contact"
                class={`block text-center py-3 rounded-xl font-bold transition-all ${
                  index === 1
                    ? 'bg-white text-primary hover:bg-white/90'
                    : 'bg-primary text-white hover:bg-primary-dark'
                }`}
              >
                Get Started
              </a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Specifications & Applications -->
    <section class="section-padding bg-slate-50 bg-pattern-dots">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-16">
          <div>
            <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 mb-4">
              <span class="w-2 h-2 rounded-full bg-primary"></span>
              <span class="text-sm font-semibold text-primary">Equipment</span>
            </div>
            <h2 class="text-3xl md:text-4xl font-bold text-slate-900 mb-8">
              Available Equipment
            </h2>

            <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
              {specifications.map((spec) => (
                <div class="flex items-center gap-3 p-4 bg-white rounded-xl shadow-sm">
                  <div class="w-8 h-8 bg-primary/10 rounded-lg flex items-center justify-center flex-shrink-0">
                    <Check class="w-5 h-5 text-primary" />
                  </div>
                  <span class="text-slate-700 font-medium">{spec}</span>
                </div>
              ))}
            </div>
          </div>

          <div>
            <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary/10 mb-4">
              <span class="w-2 h-2 rounded-full bg-primary"></span>
              <span class="text-sm font-semibold text-primary">Industries</span>
            </div>
            <h2 class="text-3xl md:text-4xl font-bold text-slate-900 mb-8">
              Who We Serve
            </h2>

            <div class="space-y-4">
              {applications.map((app) => (
                <div class="p-6 bg-white rounded-2xl shadow-sm hover:shadow-md transition-shadow">
                  <h3 class="text-lg font-bold text-slate-900 mb-2">{app.title}</h3>
                  <p class="text-slate-600">{app.description}</p>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Recent Projects -->
    <RecentProjects category="all" />

    <!-- CTA Section -->
    <section class="section-padding bg-[#1c6cb1] relative overflow-hidden">
      <div class="absolute inset-0 bg-pattern-shields opacity-10"></div>
      <div class="absolute top-0 right-0 w-96 h-96 bg-[#5a9fd4]/30 rounded-full blur-[120px]"></div>

      <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold mb-6" style="color: white;">
          Start Printing Smarter
        </h2>
        <p class="text-lg mb-8 max-w-2xl mx-auto" style="color: rgba(255,255,255,0.85);">
          Contact us today to discuss your printing needs and find the perfect leasing solution for your business.
        </p>
        <a
          href="/contact"
          class="inline-flex items-center gap-2 bg-white hover:bg-white/90 text-[#1c6cb1] px-8 py-4 rounded-xl font-bold text-lg transition-all shadow-lg group"
        >
          Contact Us Today
          <ArrowRight class="w-5 h-5 transition-transform group-hover:translate-x-1" />
        </a>
      </div>
    </section>
  </main>

  <Footer />
</Layout>
